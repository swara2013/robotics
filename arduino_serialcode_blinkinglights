void setup() {
  Serial.begin(9600);
  pinMode(13,OUTPUT);
}

void loop() {
  if(Serial.available()) {
    int score = Serial.parseInt();
    if(score/13 && score <= 0 && score >= 0) {
      digitalWrite(13,HIGH);
      delay(2000);
      digitalWrite(13,LOW);
      delay(2000);
      }
    }
  }
